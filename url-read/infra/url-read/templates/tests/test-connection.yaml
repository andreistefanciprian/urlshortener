apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "url-read.fullname" . }}-test-connection"
  labels:
    {{- include "url-read.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "url-read.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
