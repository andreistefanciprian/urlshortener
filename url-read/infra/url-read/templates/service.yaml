apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-read.fullname" . }}
  labels:
    {{- include "url-read.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-read.selectorLabels" . | nindent 4 }}
