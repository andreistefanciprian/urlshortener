apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "url-read.fullname" . }}-config
  labels:
    {{- include "url-read.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}