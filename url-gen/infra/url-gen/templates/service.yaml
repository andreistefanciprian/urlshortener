apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-gen.fullname" . }}
  labels:
    {{- include "url-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-gen.selectorLabels" . | nindent 4 }}
