apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "url-gen.fullname" . }}-test-connection"
  labels:
    {{- include "url-gen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "url-gen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
