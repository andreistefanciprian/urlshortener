apiVersion: v1
kind: Secret
metadata:
  name: {{ include "url-gen.fullname" . }}-secret
  labels:
    {{- include "url-gen.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}