[project]
name = "mcp-urlshortener"
version = "0.1.0"
description = "MCP server for urlshortener app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.27.0",
]
