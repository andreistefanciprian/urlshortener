apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    # Image updates managed by Renovate; removed Argo CD Image Updater annotations
spec:
  project: urlshortener
  source:
    repoURL: oci://ghcr.io/andreistefanciprian/urlshortener/frontend
    chart: frontend
    targetRevision: 0.1.4
    helm:
      values: |
        image:
          repository: andreistefanciprian/urlshortener-frontend
          tag: latest@sha256:1578bfc3974774f4494f90d26819e6673b98d529f5412b3862a9ddb7d4485a6e
        config:
          LOG_LEVEL: DEBUG
          BACKEND_HOST: api-gateway.api-gateway.svc
          BACKEND_PORT: "8080"
          FRONTEND_PORT: "8090"
  destination:
    server: https://kubernetes.default.svc
    namespace: frontend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
